# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Local env (secrets / placeholders — never commit)
.env
.env.local

# Live-test env (real wallet key — never commit)
test/live/.env
test/live/txns.txt
test/live/imperial-debug.jsonl
test-wallet.json

# Supabase CLI local state — never commit
supabase/.branches/
supabase/.temp/

# Snapshot artifacts from /imperial /phoenix /flash discovery scripts
# (raw API dumps, regenerated on demand — do not pin)
.imperial-openapi.json
.phoenix-markets.json
.flash-markets.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
test/live/imperial-bug-report.md
